import {StyleSheet} from 'react-native';

const Styles = StyleSheet.create({
  container: {
    padding: 8,
    fontSize: 16,
    color: 'white',
    borderWidth: 1,
    borderRadius: 4,
    borderColor: 'rgba(114, 102, 102, 0.5)',
  },
});

export default Styles;
