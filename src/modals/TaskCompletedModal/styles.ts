import {StyleSheet} from 'react-native';

const Styles = StyleSheet.create({
  container: {
    backgroundColor: '#222222',
  },

  contentContainer: {
    padding: 16,
    rowGap: 16,
  },

  button: {flexDirection: 'row', marginTop: 12},
});

export default Styles;
